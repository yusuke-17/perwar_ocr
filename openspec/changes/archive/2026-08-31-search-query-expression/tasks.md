## 1. クエリ解析の土台

- [x] 1.1 `utils/library_search.py` に `QuerySyntaxError`（`LibrarySearchError` を親に）を追加し、
      既存の例外クラス定義の並びに揃っていることを確認する
- [x] 1.2 カラム別名表（`原文/元/original → original`、`口語/現代/modern → modern`、
      `題名/表題/title → title`）を定数として定義し、`_FTS_FIELDS` のカラム番号と対応づく
      ことを単体テストで確認する
- [x] 1.3 クエリ文字列を「検索語群 / 結合モード(AND|OR) / 除外語群 / カラム限定」へ分解する
      解析関数を新設し、以下がテストで通ることを確認する:
      空白区切りのAND、`OR` を含む場合の全体OR化、`NOT` 以降の除外語収集、
      `原文:語` と `原文：語`（全角コロン）の限定、`午前10:30` が限定でなく検索語になること
- [x] 1.4 解析関数が異常入力を `QuerySyntaxError` で拒否することをテストで確認する:
      除外語のみ、`NOT` が2回以上、別名表に無いカラム名（メッセージに使える別名が並ぶこと）

## 2. FTS5 式の組み立て

- [x] 2.1 `_build_match_expr()` を解析結果から式を組み立てる形に置き換え、
      `("A" AND "B") NOT ("C" OR "D")` / `("A" OR "B")` / `({original} : "A") AND ("B")`
      の各形が生成されることをテストで確認する
- [x] 2.2 検索語・除外語・限定語のすべてに `normalize_query()` が適用されることを、
      旧字体（`關東地方`）・歴史的仮名遣いを含むケースのテストで確認する
- [x] 2.3 `QueryTooShortError` のメッセージに語の役割（検索語／除外語／限定語）を含め、
      正規化後の語も示すよう変更し、役割ごとのテストが通ることを確認する
- [x] 2.4 生成した式が実際に SQLite FTS5 で構文エラーにならないことを、
      インメモリDBに投入したテストコーパスに対する統合テストで確認する

## 3. 検索実行と結果の取得

- [x] 3.1 `search()` に抜粋長の引数を追加し、`snippet(search, -1, マーカ, マーカ, '…', N)` の
      N として渡す。零以下は `ValueError`、64 超は 64 に丸めることをテストで確認する
- [x] 3.2 `snippet()` の開始・終了記号を `[` `]` から既存の `_MARK_OPEN` / `_MARK_CLOSE`
      （`\x02` / `\x03`）へ変更し、返る抜粋にマーカが入ることをテストで確認する
- [x] 3.3 一致回数を SQL 内で数える式（`length(highlight(...)) - length(replace(...))`）へ
      置き換え、`SearchHit` に対象ごとの一致回数フィールドを追加する。
      同一カラムに3箇所一致する文書で回数が 3 になることをテストで確認する
- [x] 3.4 `matched_fields` を一致回数（> 0）から導出するよう変更し、
      既存の「原文のみ一致」判定のテストが引き続き通ることを確認する

## 4. 表示層（ANSI・JSON）

- [x] 4.1 `utils/terminal.py` を新設し、色の可否判定（`--no-color` / `NO_COLOR` / 非TTY / 設定）と
      ANSI 定数を集約する。判定の4条件それぞれについてテストが通ることを確認する
- [x] 4.2 `scripts/diff_viewer.py` の色判定・ANSI 定数を `utils/terminal.py` 参照へ置き換え、
      既存の `tests/test_diff_viewer.py` が全件通ることを確認する
- [x] 4.3 マーカ付き抜粋を ANSI 色付きテキストへ変換する関数を実装する。
      対にならない孤立マーカは強調として扱わず素の文字として落とすこと、
      色OFF時はマーカが除去され本文が色付き時と一致することをテストで確認する
- [x] 4.4 マーカ付き抜粋を「素の抜粋 + 一致箇所の位置リスト」へ変換する関数を実装し、
      本文に角括弧を含む資料で位置がずれないことをテストで確認する
- [x] 4.5 `scripts/library.py` の `cmd_find()` を 4.3 経由の出力に変更し、
      対象ごとの一致回数を結果に表示する。bm25 の値は表示しないことを確認する
- [x] 4.6 `_hit_to_dict()` を 4.4 経由に変更し、JSON の `snippet` から `[ ]` が消え、
      一致位置と対象ごとの一致回数が含まれることをテストで確認する

## 5. CLI・設定・対話メニュー

- [x] 5.1 `utils/config.py` の `_DEFAULTS` の `[search]` に抜粋長の既定値（40）を追加し、
      `config.toml` にコメント付きで追記する。`CONFIG.get("search.snippet_chars")` が
      既定値を返すことを確認する
- [x] 5.2 `scripts/library.py` の `add_find_arguments()` に抜粋長オプションを追加し、
      既定が設定値から来ることを確認する
- [x] 5.3 `add_find_arguments()` のヘルプと `parse_args()` のエピローグに
      `OR` / `NOT` / `原文:` の使用例を追加し、`--help` の出力を目視確認する
- [x] 5.4 `OR` / `NOT` / `原文:語` を含む引数が `nargs="+"` の位置引数として
      argparse を素通りすることを、CLI 引数パースのテストで確認する
- [x] 5.5 `scripts/cli.py` の `_menu_search()` のプロンプト文言を新構文に合わせて更新し、
      演算子を含む入力が `split()` / `join()` の往復で壊れないことをテストで確認する

## 6. 統合確認とドキュメント

- [x] 6.1 旧字体・歴史的仮名遣いを含むテスト用コーパス（インメモリまたは一時ディレクトリの
      library）を用意し、spec の各シナリオに対応する統合テストを追加して全件通すこと
- [x] 6.2 空白区切りの AND 検索が従来と同一の結果・同一の並び順を返すことを回帰テストで確認する
- [x] 6.3 `INDEX_SCHEMA_VERSION` が据え置きであること、および既存の索引DBを
      作り直さずに新しい検索が動くことをテストで確認する
- [x] 6.4 `uv run pytest tests/ pkg/` が全件通ることを確認する
- [x] 6.5 `README.md` の検索の節に新構文（`OR` / `NOT` / `原文:` / 抜粋長）と、
      JSON 出力の `snippet` から `[ ]` が消えた旨を追記する
- [x] 6.6 `PROGRESS.md` に本変更の完了を1行で追記し、`plan/improvement-ideas.md` の
      C2・C3 に実装済みの印とファセットを別扱いにした旨を反映する
