# prewar-ocr 改善案リスト

最終更新: 2026-07-04（G1完了）

現状調査（CLI/全体・OCRパイプライン・検索/ライブラリ）を踏まえた「あったら良さそうな実装案」。
採用 → 優先順位付け → 設計、の叩き台として使う。

各案の凡例:
- 効き: ★★★（大）／★★☆（中）
- コスト: 小 / 中 / 大（実装の手間の目安）
- 方針: 効き★☆☆（小）の案はこのリストから除外し、★★以上のみを管理する

---

## 現状の総評

基幹パイプライン「GLM-OCR → senzen_word正規化 → qwen3.5口語化 → library保存 → FTS5検索」は
堅実に完成済み。完全ローカル実行も達成済み。
改善案は「土台を壊す」ものではなく「精度・効率・使い勝手を一段上げる」増分。

---

## A. OCR精度を上げる

| #  | 案 | 中身 | 効き | コスト |
|----|----|------|------|--------|
| A2 | 複数エンジン比較（GLM-OCR × Surya） | Suryaも依存済みだが未実装。両方走らせて差分を見る／統合する。難読箇所の発見に効く | ★★☆ | 中〜大 |
| A3 | 右横書き・回転の自動判定 | 戦前文書の制約として明記の対象。ページ方向検出と回転補正がまだない | ★★☆ | 中 |
| A4 | OCR誤読辞書の拡充＋自動学習 | 現状47パターンの辞書＋文脈2パターンのみ。ユーザー修正を辞書に還元する仕組み | ★★☆ | 小〜中 |

## B. 後処理・口語化の質

| #  | 案 | 中身 | 効き | コスト |
|----|----|------|------|--------|
| B2 | 口語化プロンプトの調整可能化 | temperature等やfew-shot例がコード固定。設定で差し替えられると検証が回せる | ★★☆ | 小 |

## C. 検索・ライブラリ

| #  | 案 | 中身 | 効き | コスト |
|----|----|------|------|--------|
| C2 | OR/NOT検索・ファセット（日付・タグ） | 現状AND検索のみ。FTS5の機能を素直にCLI公開するだけ | ★★☆ | 小〜中 |
| C3 | スニペット改善 | 16文字固定・括弧ハイライト。ANSIカラー化、長さ可変、スコア表示 | ★★☆ | 小 |
| C4 | タグ付け・メモのCLI編集 | meta.json に tags/note の枠はあるが入力導線がない。後から検索性が上がる | ★★☆ | 中 |

## D. CLI・UX

| #  | 案 | 中身 | 効き | コスト |
|----|----|------|------|--------|
| D3 | 処理ログのファイル保存 | 全部 print で揮発。logging でファイル出力すると後から追える | ★★☆ | 小 |
| D4 | OCR結果の再処理・再開 | 中断したバッチの再開、同一画像のキャッシュがない | ★★☆ | 中 |

## E. 基盤・品質

| #  | 案 | 中身 | 効き | コスト |
|----|----|------|------|--------|
| E1 | CLI側のテスト整備 | senzen_word にはテストあり、scripts/utils 側は皆無。リファクタの安全網 | ★★☆ | 中 |

## F. NotebookLM的な対話・要約（新規要望）

| #  | 案 | 中身 | 効き | コスト |
|----|----|------|------|--------|
| F1 | ライブラリ横断のチャット要約（ローカルRAG） | 蓄積済み library のテキストをLLMに渡し、チャットで要約・質問応答。完全ローカルで実現可能 | ★★★ | 中〜大 |
| F2 | セマンティック検索（埋め込み） | FTS5の語句一致に加え、意味の近さで検索。Ollamaの埋め込みモデルで実現 | ★★☆ | 中 |
| F3 | 出典付き回答（引用元の明示） | 回答にどの文書のどの箇所を使ったか付与。誤要約（ハルシネーション）対策 | ★★☆ | 中 |

詳細は後述「NotebookLM的機能の実現性」を参照。

## G. コード品質・堅牢性（ソースコードレビュー由来）

現行ソースを精読して見つかった、既存案（A〜F）に載っていない実装上の問題。
「あったら良い機能」ではなく「今あるコードの弱点・潜在バグ」。行番号は調査時点。

| #  | 案 | 中身 | 効き | コスト |
|----|----|------|------|--------|
| ~~G1~~ ✅ | ~~句読点なし長文のチャンク未分割バグ~~（実装済み） | `text_modernizer.py:151-185` の `_split_text` は「。」でのみ文を切る。句読点の乏しい戦前文書では全文が1チャンクになり、`chunk_size` 超過分が qwen3.5 に渡って**無言で切り捨てられ後半が変換されない**。1文が上限超えでも分割されない。→ **対応済み**: `_split_oversized`／`_split_keep_delims` を追加し、「。」→「、」「改行」→文字数の多段フォールバック分割で全チャンクを `chunk_size` 以下に保証。強制カット時は⚠表示。回帰テスト `tests/test_text_modernizer.py`（6件）＋実LLM E2Eで欠落ゼロ確認済み。 | ★★★ | 小〜中 |
| G2 | バッチ/複数枚OCRが1枚失敗で全件破棄 | `ocr_vision_llm.py:448-456`（`--separate` も同様）。10枚中10枚目で失敗すると `return 1` で**成功済み9枚も保存されず消える**。1枚数十秒〜数分の処理で被害大。→ 失敗ページはスキップ記録し、成功分だけ save まで到達させる（D4「再開」の手前の堅牢性） | ★★☆ | 中 |
| G3 | 変体仮名変換がパイプライン未接続 | `text_normalizer.py:27-28`。senzen_word 公式 `convert()` は「旧字体→変体仮名→歴史的仮名→カタカナ助詞」順で適用する設計だが、PJ側は個別関数のみ import し `convert_hentaigana` を一度も呼んでいない。テスト済み機能が死んでおり、パッケージ意図と実装が不整合。→ `normalize_text`/`normalize_query` に追加 | ★★☆ | 小 |
| G4 | OCR呼び出しに temperature/timeout 未指定 | `ollama_client.py:160-174`。口語化側は `options` を渡すのにOCR側は無指定＝モデル既定 temperature で走り**同じ画像でも結果がぶれる**（OCRは temperature=0 が正）。全 Ollama 呼び出しに timeout が無く、固まると**無限ハング**。→ OCR用 options を config 化＋ timeout 設定 | ★★☆ | 小 |
| G5 | 検索インデックスが口語化後テキストのみ | `library_search.py:304-307`。FTS5に入るのは modern.txt（LLMが言い換えた後）だけ。LLMが変えた語は原文の語で検索してもヒットしない。研究アーカイブの再現率を損なう。→ 正規化した原文カラムも索引に追加（C1のクエリ正規化とは別軸＝「何を索引するか」） | ★★☆ | 小〜中 |
| G6 | 差分更新が meta.json の mtime のみ判定 | `library_search.py:108-133`。索引対象は modern.txt なのに変更検知は meta.json の mtime だけ。**modern.txt を手修正しても再インデックスされず**古い本文で検索し続ける。→ modern.txt の mtime／内容ハッシュも検知に含める | ★★☆ | 小 |
| G7 | `process_single`／`process_batch` の大量重複 | `ocr_vision_llm.py:339-429` と `:432-534`。正規化・口語化・レコード生成がほぼ丸ごとコピペ。片方だけ直して反映漏れ→バグ温床。テストも無く回帰検知不可（E1と補完関係）。→ 共通ヘルパーに抽出し経路を1本化 | ★★☆ | 中 |
| G8 | デッドコード・不要依存の整理 | `chunk.overlap`（config/param/コメントにあるが `_split_text` で未使用＝効くと誤解を招く）、`requests`（pyproject にあるが import 0件）、`surya-ocr`（実処理未使用の重量級必須依存）。→ overlap は実装 or 削除、requests は依存から除去、surya は optional グループへ | ★★☆ | 小 |

---

## おすすめ優先順位の叩き台

実装済み: C1 / B1 / D1 / A1 / D2 / **G1**。

### 第1優先：静かに壊れるバグを止める（既存機能の信頼性）
まず「エラーにならず結果だけ欠ける」タイプを潰す。動いて見えるのに出力が劣化する
ため最も危険で、対象文書・運用形態に直結する。

1. ~~G1 句読点なし長文のチャンク未分割~~ ✅ **完了** — 多段フォールバック分割で後半欠落を根絶
2. G2 バッチ1枚失敗で全件破棄 — 撮りため大量処理でのデータ損失を防ぐ ← **次の候補**
3. G4 OCRの temperature/timeout — 再現性の確保とハング防止（コスト小）
4. G3 変体仮名の未接続 — テスト済み機能を繋ぐだけ（コスト小）

### 第2優先：検索・索引の正確性
5. G5 原文も索引に追加 — 口語化で消えた語の取りこぼしを直す
6. G6 modern.txt 手修正の再索引 — 誤記録修正が検索に反映される
7. C2 OR/NOT検索・C3 スニペット改善 — FTS5機能の素直な公開でUX向上

### 第3優先：保守性の土台（以降の改修を安全に）
8. G7 process_single/batch の重複解消 ＋ E1 CLIテスト整備 — セットで安全網を作る
9. G8 デッドコード・不要依存の整理 — 混乱の元を除去（コスト小）
10. D3 ログのファイル保存 — 運用性

### 第4優先：新価値（土台が整ってから本丸へ）
11. F1 チャット要約（ローカルRAG） — 価値は最大級。設定外部化・検索正規化が済み着手可能
12. F2 セマンティック検索 → F3 出典付き回答 — F1の精度と信頼性を段階強化

A2（複数エンジン）・A3（右横書き）は「効きは大きいがコストも大きい本丸」。
G1〜G8で足回りを固めた後の着手が安全。

---

## NotebookLM的機能の実現性（案F 詳細）

### 結論
完全に実装可能。しかもこのプロジェクトと相性が良い。
理由は、RAG（検索拡張生成）に必要な部品がすでに揃っているため。

### RAGとは
「質問 → 関連文書を検索 → その本文をLLMに渡して回答させる」仕組み。
LLMの知識だけで答えさせず、手元の資料を根拠に答えさせる。NotebookLMもこの方式。

### 既にある部品（流用できる資産）
- 蓄積データ: library/ の modern.txt（正規化済みテキスト群）
- 検索: SQLite FTS5（質問に関連する文書を引ける）
- ローカルLLM: Ollama + qwen3.5:9b（要約・回答の生成役）
- 制約クリア: すべてローカル完結 → 「外部送信禁止」を満たす（NotebookLMはクラウド送信のためそのままでは使えないが、自前ローカルRAGなら制約に抵触しない）

### 最小構成（まず動かす版）
1. ユーザーが質問を入力
2. 質問を FTS5 で検索し、関連文書の本文を数件取得（C1のクエリ正規化を併用すると精度UP）
3. 取得本文＋質問を qwen3.5 のプロンプトに詰めて回答生成
4. 回答と「使った文書（出典）」を表示

→ 既存資産の組み合わせだけで成立。新規依存ほぼゼロ。

### 発展構成（精度を上げる版）
- F2 セマンティック検索: Ollamaの埋め込みモデル（例 nomic-embed-text / bge-m3）で
  各文書をベクトル化して保存。意味の近さで検索 → 語句が一致しなくても拾える。
  保存先はSQLite（sqlite-vec拡張）かchromadb等。
- F3 出典付き回答: 回答の根拠箇所を文書ID付きで明示 → 誤要約の検証が可能。
- 会話履歴の保持: 直前のやり取りを覚えて追加質問に答える（チャットらしさ）。

### 技術的な注意点
- コンテキスト長: 文書を全部詰めると qwen3.5 の入力上限を超える。
  検索で件数を絞る／長文はチャンク分割（既存 text_modernizer のチャンク機構が流用可能）。
- 要約の正確性: ローカル9Bモデルは大規模クラウドモデルより要約力が劣る。
  出典付き（F3）にして「モデルの作文」と「資料の事実」を区別できるようにすると安全。
- 速度: ローカルLLMは応答に時間がかかる → 進捗表示（D2）があると体感が良い。

### 推奨ステップ
1. 先に C1（クエリ正規化）を入れて検索精度を底上げ
2. F1 最小構成（FTS5検索 + qwen3.5要約）で「動くチャット」を作る
3. 物足りなければ F2（セマンティック検索）→ F3（出典明示）と段階的に強化
